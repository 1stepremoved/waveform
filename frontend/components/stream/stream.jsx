import React from 'react';

class Stream extends React.Component {

  render() {
    return (
      <main id="stream-container">
        <div id="stream-default-message">
          <p>You aren't following anyone.</p>
          <p>Just do it, it sounds weirder than it is.</p>
        </div>
      </main>
    );
  }

}

export default Stream;
